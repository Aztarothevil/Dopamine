<baseviews:MiniPlayerViewBase x:Class="Dopamine.Views.MiniPlayer.MediumPlayer"
                              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                              xmlns:utils="clr-namespace:Dopamine.Utils"
                              xmlns:commonviews="clr-namespace:Dopamine.Views.Common"
                              xmlns:baseviews="clr-namespace:Dopamine.Views.Common.Base"
                              xmlns:cp="clr-namespace:Dopamine.Core.Prism;assembly=Dopamine.Core"
                              xmlns:prismMvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Wpf"
                              mc:Ignorable="d"
                              d:DesignHeight="300" d:DesignWidth="300"
                              prismMvvm:ViewModelLocator.AutoWireViewModel="True"
                              Background="{DynamicResource Brush_WindowHeaderBackground}">

    <StackPanel  MouseLeftButtonDown="CoverGrid_MouseLeftButtonDown">
        <!-- Covert art -->
        <Grid x:Name="CoverGrid">
            <DockPanel>
                <Grid>
                    <!-- Background -->
                    <Border Panel.ZIndex="0" ClipToBounds="True">
                        <commonviews:BackgroundCoverArtControl x:Name="BackgroundCoverArtControl" />
                    </Border>
                    <Border Panel.ZIndex="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource Brush_NowPlayingBackground}"/>
                    <DockPanel Panel.ZIndex="2" x:Name="PlaylistPanel" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <commonviews:LyricsControlMedium />
                    </DockPanel>
                </Grid>
            </DockPanel>
            <!-- Cover controls -->
            <DockPanel x:Name="CoverControls" Panel.ZIndex="2" Opacity="1" VerticalAlignment="Top">
                <Grid>
                    <Border Panel.ZIndex="0" Height="29" HorizontalAlignment="Stretch" Background="{DynamicResource Brush_TopBlackBottomTransparent}"/>
                    <commonviews:WindowControls Panel.ZIndex="1" ButtonHeight="29" ButtonWidth="45" EnableHighContrast="True"
                                                HorizontalAlignment="Right" VerticalAlignment="Top" Cursor="Arrow"/>
                </Grid>
            </DockPanel>
        </Grid>
        <!-- Progress bar -->
        <commonviews:ProgressControls Height="14" Margin="0,-6,0,0"/>
        <!-- Controls -->
        <Grid>
            <commonviews:CoverPlayerControls Panel.ZIndex="1" Margin="0,-3,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>
    </StackPanel>
</baseviews:MiniPlayerViewBase>
